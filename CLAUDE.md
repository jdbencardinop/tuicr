# CLAUDE.md

Single source of truth: [`AGENTS.md`](AGENTS.md).

For fork-specific rules (architecture, canonical `ReviewStore`, provider
no-silent-loss rules, remote-write approval, build/test commands, known
pre-existing test failure, `tpatch` lifecycle, Wayfinder conventions,
commit/safety rules), go straight to
[`docs/fork/AGENT-WORKFLOW.md`](docs/fork/AGENT-WORKFLOW.md).

This file intentionally carries no separate guidance to avoid drift between
agent instructions.
