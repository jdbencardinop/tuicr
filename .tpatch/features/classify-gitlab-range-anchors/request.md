# Feature Request: Classify GitLab review threads as outdated when provider position head SHA differs from the current merge-request head, preserve current ranges, add regression tests, and leave the disposable live GitLab rerun explicitly blocked.

**Slug**: `classify-gitlab-range-anchors`
**Created**: 2026-08-12T10:10:24Z

## Description

Classify GitLab review threads as outdated when provider position head SHA differs from the current merge-request head, preserve current ranges, add regression tests, and leave the disposable live GitLab rerun explicitly blocked.
